{
  "name": "volttune",
  "version": "1.0.0",
  "description": "VoltTune — Scooter Tuning App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static app — no build needed'",
    "cap:add:ios": "npx @capacitor/cli add ios",
    "cap:sync": "npx @capacitor/cli sync",
    "cap:open:ios": "npx @capacitor/cli open ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor-community/bluetooth-le": "^4.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
